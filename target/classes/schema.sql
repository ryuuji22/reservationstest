
CREATE TABLE RESERVATION(
  RESERVATION_ID character varying(255) COLLATE pg_catalog."default" NOT NULL PRIMARY KEY,
  LICENSE_PLATE VARCHAR NOT NULL,
  STATUS VARCHAR NOT NULL,
  RES_DATE TIMESTAMP WITHOUT TIME ZONE NOT NULL
);

CREATE INDEX IDX_RES_DATE_ ON RESERVATION(RES_DATE);
